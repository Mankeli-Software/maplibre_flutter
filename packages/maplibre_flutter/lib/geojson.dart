/// Typed GeoJSON: the seven RFC 7946 geometries, `Feature`,
/// `FeatureCollection`, and the [QueriedFeature] the map's query methods return.
///
/// Everything here is also exported from `package:maplibre_flutter`; import
/// this sub-library when you want the data types on their own — building
/// features on an isolate, say, or in a package that has no widgets.
library;

export 'package:maplibre_flutter_platform_interface/geojson.dart';
