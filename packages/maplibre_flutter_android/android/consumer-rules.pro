# Consumer R8/ProGuard rules shipped to apps that depend on this plugin.
#
# The MapLibre Android SDK ships its own consumer rules, so nothing is needed
# for it here. Keep rules for the jnigen-bound classes land with milestone B
# (CLAUDE.md §5a/§9: @Keep + verify minified release builds).
