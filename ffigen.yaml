# Run with `dart run ffigen --config ffigen.yaml`.
name: MaplibreNativeBindings
description: |
  Bindings for `src/maplibre_flutter.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/maplibre_flutter_bindings_generated.dart'
headers:
  entry-points:
    - 'src/maplibre_flutter.h'
  include-directives:
    - 'src/maplibre_flutter.h'
ffi-native:
comments:
  style: any
  length: full
